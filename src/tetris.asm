code
;Recomendo utilizarmos o login do ecomp como inicial de cada rótulo para não
;ter o risco de termos rótulos duplicados.
FMG_TETRIS_MAIN:
    MOV A, #10h
    MOV A, #10h
    MOV A, #10h
    MOV A, #10h
    ret;